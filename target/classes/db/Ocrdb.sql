DROP TABLE IF EXISTS SAMPLE;

CREATE TABLE SAMPLE (
   ID INT(12) PRIMARY KEY,
   TITLE VARCHAR(200),
   REG_USER VARCHAR(20),
   DESCRIPTION VARCHAR(2000),
   REG_DATE DATE
);

INSERT INTO SAMPLE VALUES(1, 'OCR TEST', '관리자', 'TEXT DESCRIPTION', NOW());

SELECT * FROM SAMPLE;